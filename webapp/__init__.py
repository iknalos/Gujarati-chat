"""Local webapp frontend for GujaratiClaude.

Replaces the older Tkinter dashboard with a single-page HTML/CSS/JS app
served by FastAPI on localhost, wrapped in pywebview for a desktop feel.
The Python backend (claude_bridge, outputs_watcher, history) is unchanged.
"""
